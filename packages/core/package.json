{
  "name": "@morphgrid/core",
  "version": "1.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "dist-worker",
    "public/wasm"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "mocha test/*.js --timeout 10000",
    "test:node": "node --test",
    "build:worker": "tsc -p tsconfig.worker.json"
  },
  "dependencies": {
    "@morphgrid/joiner": "^1.0.1",
    "snowball-stemmers": "^0.6.0"
  },
  "devDependencies": {
    "mocha": "^11.7.1",
    "typescript": "^5.9.2"
  }
}
